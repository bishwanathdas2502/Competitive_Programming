//technique to handle delete a node


TreeNode* dfs(TreeNode* root){
    if(root == NULL)
        return NULL;

    r->left = dfs(root->left);
    r->right = dfs(root->right);

    //condition
    //true
    //return NULL

    //else
    //return root
    
}
